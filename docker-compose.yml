services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DB_HOST: 34.122.78.142 
      DB_PORT: 5432
      DB_USER: postgres 
      DB_PASSWORD: Algoma@2024 
      DB_NAME: course_tracker
      JWT_SECRET: 12DH9111hbhos5AW
      KAFKA_BROKERS: kafka:9092
      KAFKA_COURSE_TOPIC: course_updates
      SMTP_HOST: smtp.gmail.com
      SMTP_PORT: 587
      SMTP_USER: athulnarayanan62@gmail.com
      SMTP_PASS: "clxy iifi oulj bree"
      SMTP_FROM: athulnarayanan62@gmail.com
    ports:
      - "8080:8080"
    depends_on:
      database:
        condition: service_healthy
      kafka-init:
        condition: service_completed_successfully

  database:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Algoma@2024
      POSTGRES_DB: course_tracker
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db-init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    healthcheck:
      test: ["CMD", "bash", "-c", "echo ruok | nc 127.0.0.1 2181 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    depends_on:
      zookeeper:
        condition: service_started
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics --bootstrap-server kafka:9092 --list > /dev/null 2>&1"]
      interval: 10s
      timeout: 10s
      retries: 5

  kafka-init:
    image: confluentinc/cp-kafka:7.5.0
    depends_on:
      kafka:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        kafka-topics \
          --create \
          --if-not-exists \
          --topic course_updates \
          --bootstrap-server kafka:9092 \
          --replication-factor 1 \
          --partitions 1

volumes:
  db_data:
